using System;

namespace Hatgame.Multiplayer
{
    /*public class ServerMessageListener<T> : GenericListener<Action<NetworkConnectionToClient, T>> where T : struct, NetworkMessage
    {
        public void Invoke(NetworkConnectionToClient connection, T networkMessage)
        {
            _listeners?.Invoke(connection, networkMessage);
        }
    }*/
}
